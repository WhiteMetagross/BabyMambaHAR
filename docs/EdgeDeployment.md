# Edge Deployment Workflow:

The canonical BabyMamba deployment note is now maintained in [`BabyMambaEdgeDeployment.md`](BabyMambaEdgeDeployment.md).

This compatibility file has been retained so that older repository links do not break. For the full technical description of the handcrafted recurrent export path, the MambaLite-Micro style adaptations, the mixed-precision ESP32 quantization flow, and the committed implementation files, the new document should be used instead.
