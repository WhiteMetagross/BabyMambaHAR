# LightDeepConvLSTM.

A validated seed-29 LightDeepConvLSTM checkpoint and edge artifact bundle were not available in the committed baseline deployment study snapshot.

The updated `scripts/trainBaselines.py` and `scripts/runBaselineRetraining.py` files can now be used to retrain and save this baseline cleanly inside the BabyMambaHAR repository.
